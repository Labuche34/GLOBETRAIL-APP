<div class="container">
  <div class="row m-3">
    <%= simple_form_for [@travel, @message] do |f| %>
      <%= f.input :content, placeholder: "Ask a question", required: true %>
      <%= f.button :submit, 'Submit', class: 'btn btn-primary link-btn' %>
    <% end %>
  </div>
</div>
